<%= form_for(@kitchen_and_bar) do |f| %>
  <% if @kitchen_and_bar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kitchen_and_bar.errors.count, "error") %> prohibited this kitchen_and_bar from being saved:</h2>

      <ul>
      <% @kitchen_and_bar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
